0~127 ascii 문자 랜덤 생성 프로그램

1. tar -zxvf make_input.tar.gz 로 압축풀기
    - make_input.cpp
    - Makefile

2. make 로 make_input program 생성
    - make_input

3. ./make_input [!file_name] [!byte_size] [!see] 로 input data file 생성하기

*** EOF 인 ascii 26은 출력되지 않는다.
